# Dashboards + Reports 使用说明

项目已使用 Plane 原生实现 **仪表板（Dashboards）** 和 **报告（Reports）** 功能，以下说明如何访问和使用。

---

## 一、仪表板（Dashboard）

### 入口位置

- **侧边栏**：左侧边栏顶部，点击 **「首页」/「Home」**
- **URL**：`/{工作区slug}`，例如 `http://localhost:3000/my-workspace`

### 功能说明

仪表板展示工作区概览，包含：

| 组件         | 说明                     |
| ------------ | ------------------------ |
| **快捷链接** | 常用页面和操作的快捷入口 |
| **最近动态** | 最近活动、任务更新等     |
| **便签**     | 个人便签小部件           |

### 自定义仪表板

- 点击右上角 **「管理小部件」** 可开启/关闭各小部件
- 支持拖拽调整小部件顺序

---

## 二、报告（Reports / Analytics）

### 入口位置

- **侧边栏**：左侧边栏顶部，点击 **「分析」/「Analytics」**（与「首页」并列）
- **URL**：`/{工作区slug}/analytics/overview` 或 `/{工作区slug}/analytics/work-items`

### 功能说明

报告分为两个标签页：

| 标签                     | 说明                                                                   |
| ------------------------ | ---------------------------------------------------------------------- |
| **概览（Overview）**     | 工作区整体指标：用户数、项目数、工作项数、周期数等；项目洞察、活跃项目 |
| **工作项（Work Items）** | 工作项统计：创建 vs 完成趋势、自定义洞察、工作项明细表                 |

### 筛选与导出

- 使用页面顶部的 **筛选** 按钮按项目、时间范围等筛选数据
- 支持按不同维度查看和导出

---

## 三、快速访问路径

```
侧边栏顶部（始终可见）
├── 首页 (Home)        → 仪表板
├── 分析 (Analytics)   → 报告
└── ... 其他个人项
```

---

## 四、权限说明

- **仪表板**：工作区成员（Admin、Member、Guest）均可访问
- **报告**：仅 Admin、Member 可访问，Guest 无入口
